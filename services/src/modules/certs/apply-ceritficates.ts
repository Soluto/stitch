// import * as fs from 'fs/promises';
// import {certDir } from '../config';

export default function applyCertificates() {
  // TODO:
  // const files = fs.readdir(certDir);
  // logger.info('Loading cert...');
  // process.env.NODE_EXTRA_CA_CERTS = certDir;
}
